// src/routes/admin.ts
import { Router } from 'express';

const router = Router();

// Your admin-specific routes will go here
// For example: router.get('/users', adminMiddleware, ...);

export default router;